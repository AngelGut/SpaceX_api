<Window x:Class="EspaceX_api.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:EspaceX_api.ViewModels"
        xmlns:views="clr-namespace:EspaceX_api.Views"
        Title="SpaceX Explorer" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#1a1a1a">
    <Grid>
        <DockPanel>
            <Border DockPanel.Dock="Top" Background="#0066cc" Height="60" Padding="20,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="SpaceX Explorer"
                               FontSize="28" FontWeight="Bold"
                               Foreground="White" Margin="0,0,20,0"/>
                    <TextBlock Text="| Datos en vivo desde SpaceX API"
                               FontSize="14" Foreground="#cccccc"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <ContentControl Content="{Binding CurrentViewModel}">
                <ContentControl.Resources>
                    <!--<DataTemplate DataType="{x:Type vm:HomeViewModel}">
                        <views:HomeView />
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vm:LaunchesViewModel}">
                        <views:LaunchesView />
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vm:RocketsViewModel}">
                        <views:RocketsView />
                    </DataTemplate>-->
                    <DataTemplate DataType="{x:Type vm:MapViewModel}">
                        <views:MapView />
                    </DataTemplate>
                </ContentControl.Resources>
            </ContentControl>
        </DockPanel>
    </Grid>
</Window>